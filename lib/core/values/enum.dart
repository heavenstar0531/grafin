enum InputType {
  base,
  radius,
  rotation,
  axis,
  vertex,
}

enum ViewState {
  loading,
  loaded,
  empty,
  error,
}
  // rotation, axis, vertex gak ke format
  // radius keformat true
  // selain itu ( base ) keformat false